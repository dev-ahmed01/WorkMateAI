version: '3.8'

services:
  backend:
    build:
      context: ..
      dockerfile: infra/docker/backend.Dockerfile
    container_name: workmate_backend
    ports:
      - "8000:8000"
    env_file:
      - env/.env.example
    volumes:
      - ../backend/app:/app/app
    restart: unless-stopped
